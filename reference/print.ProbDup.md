# Prints summary of `ProbDup` object.

`print.ProbDup` prints to console the summary of an object of class
`ProbDup` including the method used ("a", "b" or "c"), the database
fields(columns) considered, the number of probable duplicate sets of
each kind along with the corresponding number of records.

## Usage

``` r
# S3 method for class 'ProbDup'
print(x, ...)
```

## Arguments

- x:

  An object of class `ProbDup`.

- ...:

  Unused

## See also

[`ProbDup`](https://aravind-j.github.io/PGRdup/reference/ProbDup.md)
